#doc
Надо сделать фильтр по Ctrl+A, по типу МЦ, а поля KatMC.cType там нет
#end

Alter Interface GetTekSaldoParty;

Create view
as select * from
  KatMC
, TypeMC
where ((KatMC.cType == TypeMC.nRec))
;

HandleEvent
end;

end.
